import { eq } from "drizzle-orm";
import { db } from "../db";

import { users, type User, categories } from "../../db/schema";

import { assert, expect, test } from "vitest";
import { sql } from "drizzle-orm";
import Options from "../config/options.json";

test("jsontest", async () => {});
